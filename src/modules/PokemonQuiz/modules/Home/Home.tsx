import React, { PropsWithChildren, ReactElement } from 'react';
import { View } from 'react-native';

import { styles } from './styles';

type HomePropsType = {}

export function Home(props: PropsWithChildren<HomePropsType>): ReactElement {
  return (
    <View>
      
    </View>
  );
}